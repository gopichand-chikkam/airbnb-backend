spring:
  application:
    name: airbnb

  datasource:
    url: jdbc:postgresql://localhost:5432/AirBnbDataBase
    username: postgres
    password: 282003
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

